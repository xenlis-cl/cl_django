description "Sample Django application"

start on runlevel [2345]
stop on runlevel [!2345]
respawn

setuid django
setgid django
chdir {{ django_home }}/django/
exec /usr/local/bin/uwsgi --http 127.0.0.1:8000 --home {{ django_home }}/django/djenv/example --chdir {{ django_home }}/django/django-example -w example.wsgi

